# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env*
.env.local


# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.zip

# Debug scripts and backups
scratch/
archive/
*.bak
*.tar.gz
build_output.log
backup_dashboard.zip

# Claude Code local settings (per-machine, không chia sẻ qua git)
.claude/settings.local.json

# Playwright (kết quả chạy test, không commit)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Profile Chrome riêng cho test E2E trên dữ liệu thật — chứa phiên đăng nhập, TUYỆT ĐỐI không commit
/.e2e-chrome-profile/

# Price scraper server
price-scraper-server/node_modules/
.ui-baseline/
